---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    dnf:
      update_cache: yes
      state: latest

  - name: install apache2 packge
    dnf:
      name: httpd
      state: latest

  - name: add some system monitoring tool
    dnf:
      name: htop
      state: latest
